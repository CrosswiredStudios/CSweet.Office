# Office 0.4.0

- Renew operational certificates during live control sessions without cancelling active assignments.
- Recover an expired or superseded certificate by proving possession of the original enrolled private key to headquarters with a short-lived, one-use challenge. Recovery respects approval, revocation, and drain state.
- Validate certificate responses before atomically replacing the saved identity; use the renewed certificate on new TLS connections while preserving headquarters server validation.
- Require Office.Contracts 0.6.0. Upgrade headquarters first, then drain Office and use an identity-preserving upgrade after its active assignments reach zero.

Windows recovery and TLS regression checks are included. Signed installer publishing and Linux/macOS platform certification remain part of the hardened release workflows.
