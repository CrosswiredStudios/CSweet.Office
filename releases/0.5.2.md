# Office 0.5.2

- Report the running Office assembly version on every heartbeat, allowing headquarters to detect in-place updates without re-enrollment.
- Use Office contracts 0.7.0.

- Generate release metadata from the pinned contracts version and reject release tags that differ from the runtime version.
