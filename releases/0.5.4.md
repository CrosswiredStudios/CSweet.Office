# Office 0.5.4

- Raise the guest-local broker body ceiling from 1 MiB to 16 MiB minus 64 KiB of framing headroom. This fixes coding-agent requests such as the observed 2,578,964-byte inference request being rejected before reaching the model service.
- Return HTTP 413 with a structured limit error for oversized fixed-length or chunked requests. Check smaller configured transport frames before forwarding.
- Preserve model-service limits, authorization, guest isolation, and the 16 MiB frame clamp (SEC-INV-11 and SEC-INV-12).
- Rebuild and certify the guest image and upgrade a drained Office through the normal release process. Updating the agent package alone does not replace the installed guest broker.

Source: src/CSweet.Office.RuntimeGuest/GuestLocalBrokerProxy.cs and GuestBrokerSession.cs.
Verified: 2026-09-16.

- Coordinate command-line and guided development builds with a machine-wide mutex and detection of existing build progress. Wait without terminating work; return the exact completed payload to the guided updater.
- Fix fresh-onboarding bootstrap failures caused by older progress records without ownerProcessId. Discard their running status only when a Windows reboot proves the old build stopped; retain a clear recovery error for potentially active or unverifiable records.
