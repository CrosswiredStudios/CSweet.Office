# Office 0.5.1

- Allow the three platform-provided workspace transfer limits in guest workload environments so SDK 3.45.1 agents can start. Other unapproved environment keys remain rejected.
- Runtime guest images must be rebuilt and updated to include this change.
